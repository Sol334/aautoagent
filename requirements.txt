# Galactic Capital — runtime dependencies
# Install: pip install -r requirements.txt

# ── Core ──
httpx>=0.27.0
python-dotenv>=1.0.0

# ── Market data + brokers ──
yfinance>=0.2.36
finnhub-python>=2.4.20
alpaca-py>=0.26.0
coinbase-advanced-py>=1.3.0
fredapi>=0.5.1

# ── ML models (finBERT sentiment, Granite forecast) ──
transformers>=4.40.0
torch>=2.2.0
